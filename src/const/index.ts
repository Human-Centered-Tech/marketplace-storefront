// Products per page across all listing surfaces (marketplace, category,
// collection, seller shop, search). 48 matches Etsy's grid (divisible by
// 2/3/4/6 so it lays out cleanly at any column count). Adjust here and
// every paginated grid picks it up.
export const PRODUCT_LIMIT = 48

export const PARENT_CATEGORIES = ["menswear", "womenswear"]
